# Tuples
# tuples ()
numbers= (1,2,3)

# we can not change the information inside the Tuple
print(numbers[0])

# unpacking

coordinates = (1,2,3)
x = coordinates[0]
y = coordinates[1]
z = coordinates[2]

# unpacking with tuples or lists a like as below
x, y, z = coordinates
print(x)
print(y)
print(z)
